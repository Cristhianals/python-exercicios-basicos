# Exercício 2 – Operações Matemáticas Básicas

## 🧩 Enunciado 2.1
Converta as seguintes expressões matemáticas para que possam ser calculadas usando o interpretador Python:

10 + 20 * 30
42 / 30
(94 + 2) * 6 - 1


### 🎯 Objetivo
Este exercício tem como objetivo praticar o uso dos **operadores aritméticos** em Python e compreender como a **ordem de precedência** (ou prioridade) das operações afeta o resultado final das expressões.  
Também é uma oportunidade de se familiarizar com a sintaxe do Python para operações matemáticas e o uso de parênteses para alterar a ordem dos cálculos.

---

## 🧩 Enunciado 2.2
Digite a seguinte expressão no interpretador Python e observe o resultado:

10 % 3 * 10 ** 2 + 1 - 10 * 4 / 2


### 🎯 Objetivo
Este exercício tem como objetivo reforçar o entendimento da **precedência dos operadores** e mostrar como diferentes tipos de operações — como módulo (`%`), potência (`**`), multiplicação (`*`), divisão (`/`) e soma/subtração — interagem em uma mesma expressão.  
Ao analisar o resultado, você perceberá que a **ordem de avaliação** faz toda a diferença no valor final.
